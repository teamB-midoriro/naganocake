<table class="table table-bordered">
 <thead>
  <tr>
   <td class="table-color">ジャンル検索</td>
  </tr>
 </thead>
 <tbody>
  <% genres.each do |genre| %>
  <tr>
   <td><%= link_to "#{genre.name}", genre_path(genre) %></td>
  </tr>
  <% end %>
 </tbody>
</table>
